"use client";

import { ShellTab } from "./ShellTab";

export function RevoiceTab() {
  return <ShellTab phase="Phase 7" sentence="Re-voice lands here: the same part played by another instrument, returned as editable MIDI plus audio." />;
}
