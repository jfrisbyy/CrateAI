"use client";

import { ShellTab } from "./ShellTab";

export function CompareTab() {
  return <ShellTab phase="Phase 4" sentence="Compare lands here: this file against a second one, same structure side by side, with the deltas." />;
}
