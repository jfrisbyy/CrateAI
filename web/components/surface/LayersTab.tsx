"use client";

import { ShellTab } from "./ShellTab";

export function LayersTab() {
  return <ShellTab phase="Phase 7" sentence="Layers land here: stacked lanes from different files, tempo- and key-matched, with per-lane offset, gain and stretch mode." />;
}
