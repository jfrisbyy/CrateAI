"use client";

import { ShellTab } from "./ShellTab";

export function StemsTab() {
  return <ShellTab phase="Phase 2" sentence="Stem separation lands here: drums, bass, vocals and other as child files, each with its own analysis." />;
}
